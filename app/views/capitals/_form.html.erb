<%= form_for(@capital) do |f| %>
  <% if @capital.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@capital.errors.count, "error") %> prohibited this capital from being saved:</h2>

      <ul>
      <% @capital.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">

    <%= link_to t('crud.actions.back'), capitals_path, class: 'btn btn-default', role: 'button' %>

    <%= f.submit %>
  </div>
<% end %>
